import React from 'react';

export const AiChef: React.FC = () => {
  return (
    <div className="min-h-screen bg-cc-cream flex items-center justify-center">
        <p className="text-cc-primary">Concierge services are currently unavailable.</p>
    </div>
  );
};